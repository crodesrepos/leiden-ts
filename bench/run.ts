/**
 * Benchmark entry point. Currently runs the modularity benchmark; future
 * milestones will add clustering benchmarks and the cross-implementation
 * comparison report.
 */

import './modularity.js';
